# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: student-dojo
services:  
  app:
    language: dotnet
    project: ./src/StudentDojo.AppHost/StudentDojo.AppHost.csproj
    host: containerapp
